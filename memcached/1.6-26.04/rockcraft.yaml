name: memcached
version: '1.6' 
summary: Single-line elevator pitch for your amazing rock
description: |
    This is my-rock-name's description. You have a paragraph or two to tell the
    most important story about it. Keep it under 100 words though,
    we live in tweetspace and your description wants to look good in the
    container registries out there.

base: bare
build-base: devel
run-user: _daemon_

platforms:
    amd64:

services:
  memcached:
    override: replace
    summary: "memcached service"
    command: memcached -u _daemon_
    startup: enabled

parts:
    memcached:
        plugin: nil
        stage-packages:
            - base-files_base 
            - base-files_chisel
            - base-files_release-info
            - libsasl2-modules_libs
            - memcached_bins
            - tzdata_zoneinfo

    deb-security-manifest:
        plugin: make
        source: https://github.com/canonical/rocks-security-manifest
        source-type: git
        source-branch: main
        override-prime: gen_manifest
